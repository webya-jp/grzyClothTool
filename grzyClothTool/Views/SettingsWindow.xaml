<UserControl x:Class="grzyClothTool.Views.SettingsWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:grzyClothTool.Views"
             xmlns:icons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:helpers="clr-namespace:grzyClothTool.Helpers"
             xmlns:converters="clr-namespace:grzyClothTool.Converters"
             xmlns:enums="clr-namespace:grzyClothTool"
             xmlns:c="clr-namespace:grzyClothTool.Controls"
             xmlns:loc="clr-namespace:grzyClothTool.Localization"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800"
             d:Background="{DynamicResource Brush50}">
    <Grid>
        <Grid.Resources>
            <converters:EnumToBoolConverter x:Key="EnumToBoolConverter" />
            <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <Grid Grid.Row="0">
            <Button Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Padding="5" Click="BackButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <Grid Width="30" Height="30">
                            <Ellipse Width="30" Height="30" Stroke="{DynamicResource Brush950}" StrokeThickness="2">
                                <Ellipse.Fill>
                                    <SolidColorBrush Color="{DynamicResource Color50}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <Path Stroke="{DynamicResource Brush950}" StrokeThickness="2" Fill="{DynamicResource Brush950}">
                                <Path.Data>
                                    <PathGeometry>
                                        <PathFigure StartPoint="20,9" IsClosed="True">
                                            <LineSegment Point="20,21" />
                                            <LineSegment Point="9,15" />
                                        </PathFigure>
                                    </PathGeometry>
                                </Path.Data>
                            </Path>
                        </Grid>
                        <TextBlock Foreground="{DynamicResource Brush950}" Margin="5,0,5,2" VerticalAlignment="Center" FontSize="16" FontWeight="Normal" Text="{loc:Loc Common_Back}" />
                    </StackPanel>
                </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <!--<c:CustomButton Label="PATREON ACCOUNT" Padding="10" Width="200" CornerRadius="5" MyBtnClickEvent="PatreonAccount_Click" />-->

                <ToggleButton Margin="5"  IsChecked="{Binding IsDarkMode, Mode=OneWay}" Click="ThemeModeChange_Click">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                            <Setter Property="Width" Value="100"/>
                            <Setter Property="Height" Value="25"/>
                            <Setter Property="Background" Value="{DynamicResource Brush500}"/>
                            <Setter Property="Foreground" Value="{DynamicResource Brush950}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <StackPanel Orientation="Horizontal">
                                            <!-- replaced LightbulbOn and LightbulbOff icons svg in CustomIconProvider -->
                                            <icons:MaterialIcon Kind="LightbulbOn" Width="22" Margin="3"/>
                                            <Border Width="50" Background="{TemplateBinding Background}" CornerRadius="15">
                                                <Grid>
                                                    <Ellipse x:Name="SwitchBall" Fill="{DynamicResource Brush50}" Width="11" Height="11" HorizontalAlignment="Left" Margin="5,0" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                            <icons:MaterialIcon Kind="LightbulbOff" Width="20" Margin="3"/>
                                        </StackPanel>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ThicknessAnimation Storyboard.TargetName="SwitchBall" Storyboard.TargetProperty="Margin" To="33,0,5,0" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ThicknessAnimation Storyboard.TargetName="SwitchBall" Storyboard.TargetProperty="Margin" To="5,0,33,0" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ToggleButton.Style>
                </ToggleButton>
            </StackPanel>
        </Grid>


        <Grid Grid.Row="1">
            <GroupBox Grid.ColumnSpan="2" Style="{DynamicResource FixedGroupBox}" Header="{loc:Loc Common_Settings}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="5" Grid.Column="0">

                            <GroupBox Margin="0,5" Height="120" Style="{DynamicResource FixedGroupBox}">
                                <GroupBox.Header>
                                    <TextBlock FontSize="15" FontWeight="Bold" Foreground="{DynamicResource Brush950}" Text="{loc:Loc Settings_PolygonLimitHeader}" />
                                </GroupBox.Header>
                                <Grid Margin="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto" />
                                        <RowDefinition Height="auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Padding="5,2,5,5" FontStyle="Italic" FontWeight="Normal" Foreground="{DynamicResource Brush500}" Grid.Row="0" Grid.ColumnSpan="3" TextWrapping="Wrap" Text="{loc:Loc Settings_PolygonLimitDescription}" />
                                    <c:SettingsLabelTextBox Grid.Row="1" Grid.Column="0" Label="{loc:Loc Settings_PolygonLimitHigh}" LabelFontSize="12" Text="{Binding Path=Instance.PolygonLimitHigh, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay}" ButtonVisible="False" />
                                    <c:SettingsLabelTextBox Grid.Row="1" Grid.Column="1" Label="{loc:Loc Settings_PolygonLimitMedium}" LabelFontSize="12" Text="{Binding Path=Instance.PolygonLimitMed, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay}" ButtonVisible="False" />
                                    <c:SettingsLabelTextBox Grid.Row="1" Grid.Column="2" Label="{loc:Loc Settings_PolygonLimitLow}" LabelFontSize="12" Text="{Binding Path=Instance.PolygonLimitLow, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay}" ButtonVisible="False" />
                                </Grid>
                            </GroupBox>

                            <GroupBox Margin="0,5" Height="140" Style="{DynamicResource FixedGroupBox}">
                                <GroupBox.Header>
                                    <TextBlock FontSize="15" FontWeight="Bold" Foreground="{DynamicResource Brush950}" Text="{loc:Loc Settings_TextureResolutionHeader}" />
                                </GroupBox.Header>
                                <Grid Margin="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto" />
                                        <RowDefinition Height="auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Padding="5,2,5,5" FontStyle="Italic" FontWeight="Normal" Foreground="{DynamicResource Brush500}" Grid.Row="0" Grid.ColumnSpan="3" TextWrapping="Wrap" Text="{loc:Loc Settings_TextureResolutionDescription}" />
                                    <c:SettingsLabelComboBox Grid.Row="1" Grid.Column="0" Label="{loc:Loc Settings_TextureDiffuse}" LabelFontSize="12" ItemsSource="{Binding TextureResolutionOptions}" SelectedItem="{Binding Path=Instance.TextureResolutionLimitDiffuse, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay}" />
                                    <c:SettingsLabelComboBox Grid.Row="1" Grid.Column="1" Label="{loc:Loc Settings_TextureNormal}" LabelFontSize="12" ItemsSource="{Binding TextureResolutionOptions}" SelectedItem="{Binding Path=Instance.TextureResolutionLimitNormal, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay}" />
                                    <c:SettingsLabelComboBox Grid.Row="1" Grid.Column="2" Label="{loc:Loc Settings_TextureSpecular}" LabelFontSize="12" ItemsSource="{Binding TextureResolutionOptions}" SelectedItem="{Binding Path=Instance.TextureResolutionLimitSpecular, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay}" />
                                </Grid>
                            </GroupBox>

                            <GroupBox Margin="0,5" Style="{DynamicResource FixedGroupBox}">
                                <GroupBox.Header>
                                    <TextBlock FontSize="15" FontWeight="Bold" Foreground="{DynamicResource Brush950}" Text="{loc:Loc Settings_GroupingHeader}" />
                                </GroupBox.Header>
                                <StackPanel Margin="5">
                                    <TextBlock Padding="5,2,5,5" FontStyle="Italic" FontWeight="Normal" Foreground="{DynamicResource Brush500}" TextWrapping="Wrap" Text="{loc:Loc Settings_GroupingDescription}" />
                                    <RadioButton Margin="5,3" Foreground="{DynamicResource Brush950}" Content="{loc:Loc Settings_GroupingNone}"
                                        GroupName="GroupingMode"
                                        IsChecked="{Binding Path=Instance.DrawableGroupingMode, Source={x:Static helpers:SettingsHelper.Instance}, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=None, Mode=TwoWay}" />
                                    <RadioButton Margin="5,3" Foreground="{DynamicResource Brush950}" Content="{loc:Loc Settings_GroupingByType}"
                                        GroupName="GroupingMode"
                                        IsChecked="{Binding Path=Instance.DrawableGroupingMode, Source={x:Static helpers:SettingsHelper.Instance}, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=ByType, Mode=TwoWay}" />
                                    <RadioButton Margin="5,3" Foreground="{DynamicResource Brush950}" Content="{loc:Loc Settings_GroupingByGroup}"
                                        GroupName="GroupingMode"
                                        IsChecked="{Binding Path=Instance.DrawableGroupingMode, Source={x:Static helpers:SettingsHelper.Instance}, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=GroupDefined, Mode=TwoWay}" />
                                    <RadioButton Margin="5,3" Foreground="{DynamicResource Brush950}" Content="{loc:Loc Settings_GroupingBoth}"
                                        GroupName="GroupingMode"
                                        IsChecked="{Binding Path=Instance.DrawableGroupingMode, Source={x:Static helpers:SettingsHelper.Instance}, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Both, Mode=TwoWay}" />
                                </StackPanel>
                            </GroupBox>

                            <GroupBox Margin="0,5" Style="{DynamicResource FixedGroupBox}">
                                <GroupBox.Header>
                                    <TextBlock FontSize="15" FontWeight="Bold" Foreground="{DynamicResource Brush950}" Text="{loc:Loc Settings_DrawablesPerAddonHeader}" />
                                </GroupBox.Header>
                                <StackPanel Margin="5">
                                    <TextBlock Padding="5,2,5,5" FontStyle="Italic" FontWeight="Normal" Foreground="{DynamicResource Brush500}" TextWrapping="Wrap" Text="{loc:Loc Settings_DrawablesPerAddonDescription}" />

                                    <RadioButton Margin="5,3" Foreground="{DynamicResource Brush950}" Content="{loc:Loc Settings_LimitPreset128}"
                                        GroupName="DrawableLimitPreset"
                                        IsChecked="{Binding DrawableLimitPreset, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Compatible128, Mode=TwoWay}" />
                                    <RadioButton Margin="5,3" Foreground="{DynamicResource Brush950}" Content="{loc:Loc Settings_LimitPreset255}"
                                        GroupName="DrawableLimitPreset"
                                        IsChecked="{Binding DrawableLimitPreset, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Extended255, Mode=TwoWay}" />
                                    <RadioButton Margin="5,3" Foreground="{DynamicResource Brush950}" Content="{loc:Loc Settings_LimitPresetCustom}"
                                        GroupName="DrawableLimitPreset"
                                        IsChecked="{Binding DrawableLimitPreset, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Custom, Mode=TwoWay}" />

                                    <c:SettingsLabelTextBox Margin="5"
                                        Visibility="{Binding IsCustomDrawableLimit, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Label="{loc:Loc Settings_MaxDrawableNumberLabel}" LabelFontSize="12"
                                        Text="{Binding Path=Instance.MaxDrawableNumber, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                        ButtonVisible="False" />

                                    <TextBlock Padding="5,5,5,0" TextWrapping="Wrap" FontWeight="Normal" Foreground="#C77700"
                                        Visibility="{Binding ShowExtendedLimitWarning, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Text="{loc:Loc Settings_LimitExtendedWarning}" />

                                    <TextBlock Padding="5,5,5,0" TextWrapping="Wrap" FontStyle="Italic" FontWeight="Normal" Foreground="{DynamicResource Brush500}"
                                        Text="{loc:Loc Settings_PropLimitNote}" />
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                        <StackPanel Margin="5" Grid.Column="1">
                            <GroupBox Margin="0,5" Style="{DynamicResource FixedGroupBox}">
                                <GroupBox.Header>
                                    <TextBlock FontSize="15" FontWeight="Bold" Foreground="{DynamicResource Brush950}" Text="{loc:Loc Settings_LanguageHeader}" />
                                </GroupBox.Header>
                                <StackPanel Margin="5">
                                    <c:SettingsLabelComboBox
                                        Margin="5"
                                        Label="{loc:Loc Settings_LanguageLabel}"
                                        LabelFontSize="12"
                                        ItemsSource="{Binding LanguageOptions}"
                                        SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"
                                        Description="{loc:Loc Settings_LanguageDescription}" />
                                </StackPanel>
                            </GroupBox>

                            <GroupBox Margin="0,5" Height="380" Style="{DynamicResource FixedGroupBox}">
                                <GroupBox.Header>
                                    <TextBlock FontSize="15" FontWeight="Bold" Foreground="{DynamicResource Brush950}" Text="{loc:Loc Settings_GeneralHeader}" />
                                </GroupBox.Header>

                                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel Margin="0">
                                        <c:SettingsLabelTextBox
                                            Margin="5"
                                            Label="{loc:Loc Settings_MainProjectsFolderLabel}"
                                            Text="{Binding MainProjectsFolder, Mode=TwoWay}"
                                            Description="{loc:Loc Settings_MainProjectsFolderDescription}"
                                            Title="{loc:Loc Settings_MainProjectsFolderTitle}"
                                            ButtonClick="MainProjectsFolder_Click" />

                                        <c:SettingsLabelTextBox
                                            Margin="5"
                                            Label="{loc:Loc Settings_GtavPathLabel}"
                                            Text="{Binding GTAVPath, Mode=OneWay}"
                                            Description="{loc:Loc Settings_GtavPathDescription}"
                                            Title="{loc:Loc Settings_GtavPathTitle}"
                                            ButtonClick="GTAVPath_Click" />

                                        <c:SettingsLabelCheckBox
                                            Margin="5"
                                            Label="{loc:Loc Settings_DisplayDrawablePathLabel}"
                                            Text="{loc:Loc Settings_DisplayDrawablePathText}"
                                            IsChecked="{Binding Path=Instance.DisplaySelectedDrawablePath, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay}" />

                                        <c:SettingsLabelCheckBox
                                            Margin="5"
                                            Label="{loc:Loc Settings_AutoDeleteFilesLabel}"
                                            Text="{loc:Loc Settings_AutoDeleteFilesText}"
                                            DisplayWarning="{loc:Loc Settings_AutoDeleteFilesWarning}"
                                            IsChecked="{Binding Path=Instance.AutoDeleteFiles, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay}" />

                                        <c:SettingsLabelCheckBox
                                            Margin="5"
                                            Label="{loc:Loc Settings_MarkNewDrawablesLabel}"
                                            Text="{loc:Loc Settings_MarkNewDrawablesText}"
                                            IsChecked="{Binding Path=Instance.MarkNewDrawables, Source={x:Static helpers:SettingsHelper.Instance}, Mode=TwoWay}" />
                                    </StackPanel>
                                </ScrollViewer>
                            </GroupBox>

                        </StackPanel>

                    </Grid>
                </ScrollViewer>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
